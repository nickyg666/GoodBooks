[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra -q --tb=short
markers =
    smoke: Quick endpoint smoke tests
    integration: Tests that hit live services
    slow: Tests that take a while
filterwarnings =
    ignore::DeprecationWarning
